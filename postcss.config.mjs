/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {}, // Agrega compatibilidad automática con navegadores.
  },
};

export default config;
